import React from "react";

const CustomizationRenderer = (props) => {
  const {
    customizationGroups,
    setCustomizationGroups,
    customizations,
    setCustomizations,
  } = props;
  return <div>CustomizationRenderer</div>;
};

export default CustomizationRenderer;
